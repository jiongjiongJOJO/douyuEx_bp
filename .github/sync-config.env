# 同步配置文件
# 这个文件用于配置GitHub Actions的同步行为

# 上游仓库配置
UPSTREAM_REPO="https://github.com/qianjiachun/douyuEx.git"
UPSTREAM_BRANCH="master"

# 构建配置
BUILD_COMMAND="npm run build"
SOURCE_DIST_DIR="dist"
TARGET_DIST_DIR="build-output"

# 同步配置
AUTO_SYNC_CRON="0 0 * * *"  # 每天UTC 00:00
FORCE_PUSH_ON_CONFLICT=true

# Artifacts配置
ARTIFACT_RETENTION_DAYS=30
ARTIFACT_NAME_PREFIX="douyuex-build"

# Git配置
GIT_USER_NAME="GitHub Action"
GIT_USER_EMAIL="action@github.com"
COMMIT_MESSAGE_TEMPLATE="🤖 自动同步上游代码并构建"

# 注意：修改此文件后，需要手动更新相应的workflow文件
# 这个文件仅用作参考，实际配置需要在.yml文件中修改
